---
layout: 'post'
permalink: '/git/git-ftp/'
paginate_path: '/git/:num/git-ftp/'
lang: 'ja'
categories: 'git'
comments: true

title: Git FTPを使ってファイルアップロード
description: FTPを使ってファイルをアップロードするため、Git FTPを使う方法について説明します。
image: '/assets/images/category/git/2020/git-ftp/background.jpg'
---

<div id="contents_list" markdown="1">

## 目次

- [目次](#目次)
- [概要](#概要)
- [Git FTPインストール](#git-ftpインストール)
- [使い方](#使い方)
  - [Setup](#setup)
  - [Init](#init)
  - [Catchup](#catchup)
  - [Push](#push)
- [完了](#完了)

</div>

## 概要

トーイプロジェクトを開発する時、FTPを使ってファイルをサーバへアップロードして、デプロイする必要がありました。

- [「Clog」 サービス開発日誌(React Native, Laravel, Django)]({{site.url}}/clog/development-journal/){:target="_blank"}

このようにGitで管理するファイルをFTPを使ってデプロイする場合、`Git FTP`を使うと、簡単にデプロイすることができます。

- [Git FTP](https://github.com/git-ftp/git-ftp){:rel="nofollow noreferrer noopener" target="_blank"}

## Git FTPインストール

- macOS

下記のコマンドを使ってGit FTPをインストールします。

```bash
brew install git
brew install brotli
brew install git-ftp
```

- Others

他のOSを使ってる方は、下記のリンクを参考してインストールします。

- [Git FTP Installation](https://github.com/git-ftp/git-ftp/blob/master/INSTALL.md){:rel="nofollow noreferrer noopener" target="_blank"}

{% include in-feed-ads.html %}

## 使い方

### Setup

Git FTPを使うため、下記のコマンドでFTPの情報を設定します。

```bash
git config git-ftp.url "ftp://example.com/ftp/server/address"
git config git-ftp.user "user-id"
git config git-ftp.password "user-password"
```

### Init

サーバへまだファイルをアップロードしてない場合、下記のコマンドを使って初期化や全てのファイルをアップロードします。

```bash
git ftp init
```

### Catchup

既にFTPを使ってファイルをアップロードした場合、下記のコマンドを使ってアップロードされたファイルを同期化します。

```bash
git ftp catchup
```

### Push

全ての設定が終わりました。開発をするためファイルを修正したら、普通に下記のコマンドでGitを実行します。

```bash
# Modfiy some source codes
git add .
git commit -m 'Fix some bugs'
git push origin master
```

その後、FTPを使ってファイルをアップロードするため、下記のコマンドを実行します。

```bash
git ftp push
```

## 完了

これでGit FTPを使う方法について見てみました。最近は普通FTPでファイルをアップロードしたら、デプロイしたりしないので、あまり、役に立たないと思いますが、私みたいに特別な状況の場合、結構使えると思います。
