---
layout: 'post'
permalink: '/ruby/array/'
paginate_path: '/ruby/:num/array/'
lang: 'ko'
categories: 'ruby'
comments: true

title: Ruby에서 배열
description: Ruby에서 배열을 사용하는 방법에 대해서 알아봅시다.
image: '/assets/images/category/ruby/2020/array/background.jpg'
---

<div id="contents_list" markdown="1">

## 목차

- [목차](#목차)
- [개요](#개요)
- [기본 사용법](#기본-사용법)
- [Array.new](#arraynew)
- [배열의 크기 구하기](#배열의-크기-구하기)
- [배열 삭제](#배열-삭제)

</div>

## 개요

Ruby에서 배열을 사용하는 방법에 대해서 설명합니다.

## 기본 사용법

배열은 아래와 같이 선언하고 사용하는 것이 가능합니다.

```ruby
cats = ['Nabi', 'Mini', 'Kitty']
puts cat[0]
# Navi
```

다른 언어와 동일하게 배열의 Index는 `0`부터 시작합니다.

## Array.new

배열을 선언할 때, 가장 쉬운 방법은 아래와 같습니다.

```ruby
cats = ['Nabi', 'Mini', 'Kitty']
```

하지만, `Array.new`를 사용하여 배열을 생성할 수도 있습니다.

```ruby
cats = Array.new
# []
```

위와 같이 아무것도 지정하지 않고 선언하면, 요소가 0개인 배열이 만들어집니다.

```ruby
cats = Array.new(2)
# [nil, nil]
```

위와 같이 하나의 숫자를 지정하면, 해당 숫자의 크기만큼의 배열이 생성이 되며, 초기값으로 `nil`을 가지게 됩니다.

```ruby
cats = Array.new(3, 1)
# [1, 1, 1]
```

위와 같이 선언하면, 요소의 개수(`3`)만큼 요소의 값(`1`)이 채워진 배열을 만들 수 있습니다.

## 배열의 크기 구하기

배열의 크기를 구하기 위해서 `size` / `length` 함수를 사용할 수 있습니다.

```ruby
cats = ['Nabi', 'Mini', 'Kitty']
cats.size
# 3
```

아래와 같이 `length` 함수로도 배열의 크기를 구할 수 있습니다.

```ruby
cats = ['Nabi', 'Mini', 'Kitty']
cats.length
# 3
```

## 배열 삭제

아래와 같이 `clear` 함수를 사용하여 배열의 내용을 모두 삭제할 수 있습니다.

```ruby
cats = ['Nabi', 'Mini', 'Kitty']
cats.clear
# []
```
